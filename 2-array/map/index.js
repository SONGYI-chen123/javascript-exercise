export default function numberMapToWord(collection) {
  // TODO 3: 在这里写实现代码
  let transformCollection = collection.map((element) =>{
      let transformChars = char(element+96); 
  });
  return transformCollection;
}
